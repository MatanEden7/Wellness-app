// WellnessDomain — pure logic, no SwiftUI, no SwiftData.
// The port target for ~14,000 LOC of business logic.

import Foundation
import WellnessModels

public enum WellnessDomainMarker {
    public static let version = "0.1.0"
}
