// WellnessUI — design system + shared components.
// SwiftUI only, no domain logic, no persistence.

import SwiftUI
import WellnessModels

public enum WellnessUIMarker {
    public static let version = "0.1.0"
}
