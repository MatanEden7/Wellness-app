
import { ExerciseType, MuscleGroup, Equipment, MovementPattern, UnitMode } from './types';

export const EXERCISE_TYPES = Object.values(ExerciseType);
export const MUSCLE_GROUPS = Object.values(MuscleGroup);
export const EQUIPMENT_TYPES = Object.values(Equipment);
export const MOVEMENT_PATTERNS = Object.values(MovementPattern);
export const UNIT_MODES = Object.values(UnitMode);
